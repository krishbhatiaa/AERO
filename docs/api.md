# API reference (generated)

> Generated by `python scripts/gen_api_docs.py` from the running application's OpenAPI schema. Interactive docs: `/api/v1/docs`.

## Conventions

- Base path `/api/v1`. Successful responses: `{ "data": ..., "meta": { "request_id", "page", "page_size", "total" } }`.
- Errors: RFC 7807 `application/problem+json` with `type, title, status, code, detail, request_id` (+ `errors[]` for validation). No stack traces or filesystem paths.
- Pagination: `page` (>=1), `page_size` (1-200). Sorting: `sort=-field,field`. Filtering: see each endpoint.
- Every request gets an `X-Request-ID` (a valid incoming one is honoured). Timestamps are ISO-8601 **UTC** with `Z`.
- Auth: `AUTH_MODE=none` (local dev) or `api_key` (`X-API-Key`; roles viewer < analyst < admin). Health probes are always open.
- Rate limit and body-size limit are configurable (`RATE_LIMIT_PER_MINUTE`, `MAX_BODY_BYTES`); 429 carries `Retry-After`.
- WebSocket: `GET /api/v1/ws?channels=events,alerts,jobs` -> `{type, ts_utc, request_id, payload}`; with `api_key` auth the first client message must be `{"type":"auth","api_key":"..."}`.
- All demo products carry `data_kind: SYNTHETIC_DEMO`. `severity` is an analytical category, **not** an official warning level.

## Endpoints

| Method | Path | Summary | Parameters |
|---|---|---|---|
| GET | `/api/v1/alerts` | List Alerts | `severity`, `event_id`, `sort`, `page`, `page_size` |
| GET | `/api/v1/alerts/{alert_id}` | Get Alert | `alert_id`* |
| GET | `/api/v1/alerts/{alert_id}/geojson` | Alert As Geojson | `alert_id`* |
| GET | `/api/v1/capabilities` | Capabilities |  |
| GET | `/api/v1/data-sources` | Data Sources |  |
| GET | `/api/v1/datasets` | List Datasets | `page`, `page_size` |
| GET | `/api/v1/datasets/{dataset_id}` | Get Dataset | `dataset_id`* |
| GET | `/api/v1/evaluation/downscaling` | Eval Downscaling |  |
| GET | `/api/v1/evaluation/tracking` | Eval Tracking |  |
| GET | `/api/v1/events` | List Events | `event_type`, `severity`, `data_kind`, `bbox`, `from`, `to`, `sort`, `page`, `page_size` |
| GET | `/api/v1/events/{event_id}` | Get Event Detail | `event_id`* |
| GET | `/api/v1/events/{event_id}/downscaled` | Downscaled | `event_id`* |
| GET | `/api/v1/events/{event_id}/explain` | Explain | `event_id`*, `lead_hours` |
| GET | `/api/v1/events/{event_id}/impact` | Impact | `event_id`*, `lead_hours` |
| GET | `/api/v1/events/{event_id}/trajectory` | Trajectory | `event_id`* |
| GET | `/api/v1/events/{event_id}/uncertainty` | Uncertainty | `event_id`* |
| GET | `/api/v1/fields` | Get Field | `product`, `variable`, `lead_hours`, `method` |
| GET | `/api/v1/forecasts` | List Forecasts | `page`, `page_size` |
| GET | `/api/v1/forecasts/{run_id}` | Get Forecast | `run_id`* |
| GET | `/api/v1/health` | Health |  |
| POST | `/api/v1/jobs` | Create Job |  body: `JobCreate` |
| GET | `/api/v1/jobs` | List Jobs | `page`, `page_size` |
| GET | `/api/v1/jobs/{job_id}` | Get Job | `job_id`* |
| GET | `/api/v1/live` | Live |  |
| GET | `/api/v1/model-runs` | List Model Runs | `page`, `page_size` |
| GET | `/api/v1/models` | List Models | `page`, `page_size` |
| POST | `/api/v1/predictions` | Create Prediction |  body: `PredictionCreate` |
| GET | `/api/v1/ready` | Ready |  |
| WS | `/api/v1/ws` | Live updates (events, alerts, jobs) | `channels` |

